# 공통 개발 규칙 (Claude + Gemini 공유)

> 이 파일은 Claude Code와 Gemini CLI가 함께 작업할 때 반드시 따라야 할 규칙입니다.
> 두 AI가 만든 코드가 서로 충돌하지 않도록 이 규칙을 최우선으로 지켜주세요.

---

## 1. 코드 작성 전 필수 확인

- **기존 파일을 수정하기 전에 반드시 먼저 읽을 것**
- **이미 존재하는 함수/클래스/모듈을 중복 생성하지 말 것**
- 구현 전에 `STRUCTURE.md`를 확인해 전체 구조 파악 후 작업
- 새 파일/모듈을 만들면 즉시 `STRUCTURE.md`에 추가

## 2. 네이밍 규칙

| 대상 | 규칙 | 예시 |
|------|------|------|
| 변수/함수 | camelCase | `getUserData`, `isLoading` |
| 클래스 | PascalCase | `UserService`, `ApiClient` |
| 파일명 | kebab-case | `user-service.ts`, `api-client.py` |
| 상수 | UPPER_SNAKE_CASE | `MAX_RETRY`, `API_BASE_URL` |
| 디렉토리 | kebab-case | `user-auth/`, `api-handlers/` |

## 3. 코드 스타일

- 들여쓰기: **공백 2칸** (탭 사용 금지)
- 줄 끝 공백 없음
- 파일 끝 빈 줄 1개
- 한 함수는 한 가지 일만 (단일 책임)
- 매직 넘버 금지 → 상수로 정의

## 4. 주석

- 주석은 **한국어**로 작성
- "무엇을" 하는지보다 **"왜"** 하는지를 설명
- 자명한 코드에는 주석 달지 않기
- TODO 형식: `// TODO: 구체적인 할 일`

```python
# 나쁜 예: i를 1 증가시킨다
i += 1

# 좋은 예: API 제한으로 인해 요청 간 0.5초 대기
time.sleep(0.5)
```

## 5. Git 워크플로우

### 작업 전 필수
```bash
git pull origin [내 브랜치]   # 작업 전 항상 최신 상태로
```

### 작업 단위 브랜치
- **절대 master에 직접 push 금지**
- 각자 자기 브랜치에서만 작업 (파트 배정 후 브랜치 확인)
- master 병합은 사람이 직접 확인 후 진행

### 커밋 & 푸시 순서
```bash
git pull origin [내 브랜치]   # 1. 먼저 pull
git add [파일명]              # 2. 변경 파일만 add (git add . 자제)
git commit -m "타입: 내용"    # 3. 커밋
git push origin [내 브랜치]   # 4. 내 브랜치에만 push
```

### 커밋 메시지
- 메시지는 **한국어**
- 형식: `타입: 내용`
- 타입 목록:
  - `기능`: 새 기능 추가
  - `수정`: 버그 수정
  - `리팩토링`: 기능 변경 없는 코드 개선
  - `스타일`: 코드 포맷 변경
  - `삭제`: 파일/기능 제거
- 예시: `기능: 사용자 로그인 API 구현`

### 충돌 발생 시
- 혼자 해결하려 하지 말고 사용자에게 알릴 것

## 6. AI 역할 분담 (해커톤 운영 방식)

### 메인: Claude
- 프로젝트 구조/설계
- 핵심 로직, API 통합
- 디버깅, 복잡한 문제 해결
- 코드 리뷰 및 통합

### 서브: Gemini (다음 경우에만 사용)
1. **Claude 토큰 한도 도달** → 5시간 대기 대신 임시 작업
2. **완전 독립 작업 병렬 처리** → 예: 더미 데이터 생성, UI 컴포넌트 반복 작성

### 금지 패턴
- ❌ 같은 코드를 Claude ↔ Gemini 캐치볼
- ❌ 단순 검증 목적으로 다른 AI에 보내기
- ❌ 한 파일을 두 AI가 동시 수정

## 7. AI 간 협업 규칙

- **절대 다른 AI가 만든 코드를 이유 없이 전면 재작성하지 말 것**
- 기존 코드에 문제가 있으면 수정(Edit)하되, 삭제 후 재작성하지 말 것
- 새 기능은 기존 코드 **옆에** 추가하는 방식으로
- 환경변수/설정값은 반드시 `.env` 또는 `config` 파일로 분리
- 하드코딩된 경로, URL, 비밀키 금지

## 8. 토큰 절약 규칙 (Claude 한도 관리)

### 빠른 요약 (치트시트)

| 상황 | 좋은 예 | 나쁜 예 |
|---|---|---|
| 데이터프레임 보여주기 | `df.head().to_csv('preview.csv')` 후 경로 공유 | `print(df.head())` 결과 통째로 복붙 |
| 에러 보고 | 마지막 에러 메시지 + 라인번호만 | 전체 스택 트레이스 복붙 |
| 파일 재확인 요청 | "X 함수만 봐줘" | "전체 다시 봐줘" |
| 작업 요청 | 한 번에 한 가지 | "이거 다 처리해줘" |
| 코드 수정 | "Y 부분만 수정" | "파일 전체 다시 짜줘" |
| 이미지/플롯 | 파일 저장 후 경로 전달 | 화면 출력 후 보여주기 |

### 상세 규칙

#### 큰 출력 처리
- **데이터프레임 미리보기**: `print(df.head())` 대신 `df.head().to_csv('preview.csv')` 후 파일 경로만 공유
- **이미지/플롯**: 화면 출력 X → 파일 저장 후 경로 전달
- **대용량 JSON/리스트**: 일부만 잘라서 보여주거나 파일로

### 에러 처리
- 전체 스택 트레이스 X → **마지막 에러 메시지 + 발생 줄 번호**만 복붙
- 같은 에러 반복 시: "아까 그 에러 다시 발생"으로 충분

### 파일 읽기
- 같은 파일 반복 요청 X (한 번 읽으면 컨텍스트에 있음)
- 큰 파일은 필요한 부분만 (`offset`, `limit` 활용)

### 작업 단위
- 한 번에 한 가지 작업만 요청 → 컨텍스트 분리
- "전체 다 검토해줘" 같은 무한정 요청 X → "X 함수만 검토"식으로 범위 지정

### 출력 길이
- 코드 변경 시 전체 파일 재출력 X → Edit으로 부분만
- "설명 길게" 요청 X → 한 줄 요약 기본

## 9. 에러 처리

- 무시하는 예외 처리 금지 (`except: pass`, `catch(e) {}` 형태)
- 에러 메시지는 구체적으로 작성
- 사용자 입력은 반드시 검증

## 10. 금지 사항

- 사용되지 않는 코드(dead code) 남기기
- 전역 변수 남발
- 요청하지 않은 기능 추가 (기능 추가는 명시적 지시가 있을 때만)
- 스펙에 없는 추상화/리팩토링

---

## 스택 결정 후 추가할 내용

스택이 결정되면 아래 섹션을 채워주세요:

### 프레임워크/언어
- 결정된 스택: (미정)
- 패키지 매니저: (미정)
- 린터/포맷터: (미정)

### 폴더 구조
```
(스택 결정 후 작성)
```

### 의존성 추가 규칙
- (스택 결정 후 작성)
